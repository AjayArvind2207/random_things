list_tuple = eval(input("Enter a list or a tuple"))

#Using in-built max,min functions
max_t = max(list_tuple) 
min_t = min(list_tuple)
print("Max value in the iterable =", max_t, " and Min value in the iterable =", min_t)
